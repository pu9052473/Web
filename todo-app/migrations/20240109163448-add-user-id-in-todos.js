/* eslint-disable no-unused-vars */
// this is auto generated by "npx sequelize-cli migration:create --name add-user-id-in-todos"
"use strict";

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    // this create a table name:"Todos" , column name:"userId" , attribute:{..}
    await queryInterface.addColumn("Todos", "userId", {
      type: Sequelize.DataTypes.INTEGER,
    });

    await queryInterface.addConstraint("Todos", {
      fields: ["userId"],
      type: "foreign key",
      references: {
        table: "Users",
        field: "id",
      },
    });
    /**
     * Add altering commands here.
     *
     * Example:
     * await queryInterface.createTable('users', { id: Sequelize.INTEGER });
     */
  },

  async down(queryInterface, Sequelize) {
    // again run the "npx sequelize-cli migration:create --name add-user-id-in-todos" , delete the areated column
    await queryInterface.removeColumn("Todos", "userId");
    /**
     * Add reverting commands here.
     *
     * Example:
     * await queryInterface.dropTable('users');
     */
  },
};
