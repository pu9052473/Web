/* eslint-disable no-undef */
describe("First test suite", () => {
  test("First case", () => {
    expect(true).toBe(true); // in expect we give the value , toBe we said that what value should be return , if both match then test "PASS" else "FAIL"
  });
});
